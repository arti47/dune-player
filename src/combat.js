// combat.js — conflict tracker (5 types, zones, initiative passing, defeat flow),
// generic extended-task tracker, scene/adventure lifecycle events (§3.17).
// Local helper lands in Phase 4; shared sync in Phase 5.

export function renderConflict() {
  throw new Error('Conflict tracker arrives in Phase 4.');
}
