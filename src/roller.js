// roller.js — the 2d20 engine. Lands in Phase 3.
// Will own: dice buying (1/2/3), focus crit ranges, complications,
// Momentum/Threat/Determination spends, opposed tests (defender-first),
// extended-task scoring, talent-embedded effects, assists, Architect mode,
// and roll-log writes.

export function openRollDialog() {
  throw new Error('Dice engine arrives in Phase 3.');
}
